Pseudo code for:

+ InsertionSort(A):

for i = 1 to length(A) - 1 do
   key = A[i]
   j = i - 1
   while j >= 0 and A[j] > key do
       A[j + 1] = A[j]
       j = j - 1
   end while
   A[j + 1] = key
end for


+ MergeSort(A):
    if length(A) <= 1 then
        return A

    middle = (start + end) / 2
    
    leftArray = MergeSort(array, start, middle)
    rightArray = MergeSort(array, middle+1, end)
    
    Merge(leftArray, rightArray) 

/* Here i didn't define the pseudo code for merge function 
but its pretty obvious it sorts and merges two arrays. */